/**
 * This example demonstrates the treelist widget.
 */
Ext.define('webapp.view.index.index', {
    extend: 'Ext.panel.Panel',
    requires:[
    ],
    items:[
        {
            xtype:"button",
            text:"关闭"
        }
    ]
});